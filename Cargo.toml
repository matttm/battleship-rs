    # Workspace Cargo.toml (at the root of the repository)
    [workspace]
    members = [ "battleship_client", "crates/battleship_models" ]

    [workspace.dependencies]
    serde_json = { version = "1.0.142" }
    serde = { version = "1.0.219", features = ["derive"] }
